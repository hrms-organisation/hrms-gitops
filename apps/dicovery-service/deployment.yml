apiVersion: apps/v1
kind: Deployment
metadata:
  name: discovery-service
  namespace: hrms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: discovery-service
  template:
    metadata:
      labels:
        app: discovery-service
    spec:
      containers:
        - name: discovery-service
          image: ihab0/discovery-service:latest
          ports:
            - containerPort: 8761
