apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yml
  - service.yml
  - secret.yml

configMapGenerator:
  - name: keycloak-realm-config
    files:
      - ./realm/realm-microservice.json

generatorOptions:
  disableNameSuffixHash: true
